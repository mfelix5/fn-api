const { expect } = require('chai')
const { getRecommendation } = require("../src/services/recommendationService");

xdescribe("Recommendation Service", () => {
  it("should respond to query with a purchase and travel recommendation", () => {
  });
  it("should recommend the simplest puchase option if costs are equal", () => {
  });
  it("should provide correct recommendation when discounted fares are used", () => {
  });
});