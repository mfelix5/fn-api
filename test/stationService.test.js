const { expect } = require('chai')
const stationService = require("../src/services/stationService");

xdescribe("Station Service", () => {
  it("should find stations", () => {
  });
  it("should return all lines in system", () => {
  });
  it("should return all stations on line", () =>{
  });
});