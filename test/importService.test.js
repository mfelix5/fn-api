const { expect } = require('chai')
const importService = require("../src/services/importService")

describe("Import Service", () => {
  xdescribe("NJT", () => {
    it("should create station documents with the correct fares", () => {
    });
    it("should correctly parse fare tables with different ticket types for each destination", () => {
    });
  })
  xdescribe("MTA", () => {
    it("should create station documents with correct fares", () => {
    });
  })
  xdescribe("LIRR", () => {
    it("should create station documents with correct fares", () => {
    });
  })
})